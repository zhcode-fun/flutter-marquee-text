name: marquee_text
description: Scroll automatically when the text exceeds the width of the container.
version: 1.1.0
homepage: https://github.com/zh28/flutter-marquee-text

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.9.0
